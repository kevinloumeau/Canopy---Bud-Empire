// Test-only source instrumentation: no simulator controls enter the production bundle.
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright'),fs=require('node:fs');
(async()=>{const browser=await chromium.launch({headless:true,channel:'chrome'}),page=await browser.newPage();await page.route('**/src/main.js*',async route=>{const response=await route.fetch();let source=await response.text();source=source.replace('if(document.modelContext&&document.modelContext.registerTool)',`window.__depthSim=function(product,hours,invest){state=fresh();state.gameSpeed=0;state.productMenu={active:product,unlocked:[true,true,true]};state.lines=[10,10,10,10,10,10];state.staff=[3,3,3,3,3,3];state.idStaff=5;state.kiosk=true;state.empire.reputation=80;state.money=0;customers=[];arrival=0;work.fill(0);batchRemainder.fill(0);readyWork=0;var snapshots=[];for(var n=0;n<hours*72000;n++){simulate(.05);if(invest&&n%1200===0){var slow=slowestStation(),candidates=[{price:cost(slow),apply:function(){state.lines[slow]++}},{price:staffCost(slow),apply:function(){state.staff[slow]++}}];if(state.globalLevel<BOOST_MAX)candidates.push({price:boostCost(state.globalLevel),apply:function(){state.globalLevel++;state.multiplier=Math.pow(1.25,state.globalLevel)}});candidates.sort(function(a,b){return a.price-b.price});if(state.money>=candidates[0].price){state.money-=candidates[0].price;candidates[0].apply()}}if((n+1)%72000===0)snapshots.push({hour:(n+1)/72000,cash:Math.round(state.money),earned:Math.round(state.lifetime),served:state.sold,lines:state.lines.slice(),staff:state.staff.slice(),boosts:state.globalLevel,bottleneck:slowestStation()})}tickTime=performance.now();return snapshots};if(document.modelContext&&document.modelContext.registerTool)`);await route.fulfill({response,body:source})});await page.goto(process.env.GAME_URL||'http://127.0.0.1:4176');await page.waitForFunction(()=>window.__depthSim);const report={method:'Actual simulate(.05), 1x; starts at all stations Lv10, staff3, one kiosk, security5. No online sales or branch purchases. Invest scenario buys cheapest bottleneck equipment, employee or global boost every minute when affordable.',scenarios:[]};for(let product=0;product<3;product++){for(const invest of [false,true]){const result=await page.evaluate(({product,invest})=>window.__depthSim(product,4,invest),{product,invest});report.scenarios.push({product:['Flower','Pre-rolls','Edibles'][product],invest,result});console.log(report.scenarios.at(-1))}}fs.mkdirSync('artifacts/economy',{recursive:true});fs.writeFileSync('artifacts/economy/four-hour-simulation.json',JSON.stringify(report,null,2));await browser.close()})().catch(e=>{console.error(e);process.exit(1)});
